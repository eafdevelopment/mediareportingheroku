default: &defaults

  summary_metrics:
    - impressions
    - sessions
    - clicks
    - ctr
    - cpc
    - goal_conversions
    - cost_per_goal_conversion
    - average_session_duration

  exclude_metrics:
    - cpp

development:
  <<: *defaults

test:
  <<: *defaults

production:
  <<: *defaults