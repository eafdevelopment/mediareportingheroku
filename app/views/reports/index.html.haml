.row
  .small-12.columns.searched
    %h2 All reports
.row
  .small-12.columns
    %table
      %thead
        %tr
          %th Created
          %th Report title (click to download)
          %th 
      %tbody
        - @reports.each do |report|
          %tr
            %td= report.created_at.strftime("%d/%m/%Y")
            %td
              = link_to report.csv.url do
                = report.title
            %td= link_to "X", dataset_path(report), "data-confirm": "Are you sure you want to delete this report?", method: :delete
            

    